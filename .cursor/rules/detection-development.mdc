---
description: 检测功能开发规范：项目定位、兼容性、安全设计、误报控制、日志与 PM 协作
globs: app/src/main/cpp/**/*,app/src/main/java/anti/rusda/detector/**/*
alwaysApply: false
---

# 检测功能开发规范

## 〇、项目定位与用户范围

- **项目性质**：用于检测手机环境及是否被逆向/调试工具（Frida、Xposed、ptrace、模拟器等）侵害的**检测工具 App**，实质属于**风控 SDK** 开发。
- **用户群体**：覆盖范围大，可能包含 **Android 10～16** 及多厂商设备（OPPO、Vivo、小米、华为、三星、Google Pixel 等）。
- **开发要求**：
  - 设计时**必须考虑兼容性**：不同 Android 版本、厂商 ROM、权限与 API 差异。
  - 实现时可参考**各版本/厂商的 Android 源码**（AOSP、厂商开源适配）设计逻辑与降级策略。
  - 新增权限或敏感 API 时，评估在低版本/严格权限下的行为，避免因权限不足导致崩溃或大量 WARNING。

---

## 一、安全设计：Native 优先 + syscall（风控 SDK）

**原则**：本项目为风控 SDK，代码应尽量**安全、抗篡改**：敏感逻辑放 **Native 层**，用 **syscall** 替代系统库调用，降低被 hook 篡改的风险。

### 1.1 检测实现层级：Native 优先

| 层级 | 适用场景 | 说明 |
|------|----------|------|
| **Native 层** | 文件/端口/内存/进程/系统调用等敏感操作 | ✅ 推荐：配合 syscall、符号隐藏、混淆，提高逆向难度 |
| **Java 层** | 仅依赖 Android API 的简单检测（如 Build、系统属性） | ⚠️ 可接受：易被 hook，仅用于非核心或辅助判断 |

- 新增检测先评估：能否在 `cpp/detector/` 或 `cpp/detector/env_detector.cpp` 中实现？
- 调试检测 → `DebugDetectionManager` + `native-lib.cpp`；环境检测 → `EnvDetectionManager` + `native-lib-env.cpp`
- 须同步修改：`CMakeLists.txt` 源列表、对应 JNI 桥接

### 1.2 系统调用：优先 syscall，避免 libc 被 hook

- **已有工具**：`app/src/main/cpp/utils/syscall_utils.cpp` 提供 `my_open`、`my_read`、`my_write`、`my_close`、`my_access`、`my_socket`、`my_connect` 及 `my_strcmp`、`my_strstr` 等
- **用法**：Native 检测器中 `#include "utils/syscall_utils.h"`，用 `my_*` 替代 `open/read/write/strcmp/strstr` 等
- **新增 syscall**：参考 `syscall_utils.cpp` 的 `do_syscall` 与架构宏，实现对应封装
- **字符串/内存**：使用 `my_strstr`、`my_strcmp` 等，避免直接调用 libc，降低 inline hook 风险

---

## 二、兼容性与多版本/多厂商

- **系统版本**：逻辑需在 Android 10～16 上可运行，注意 API level、行为差异（如 16KB 页面对齐、新权限模型）。
- **厂商与 ROM**：不同厂商对 `/proc`、系统属性、权限、后台限制等有差异，设计时参考相关 AOSP/厂商源码，对已知正常行为做**排除或降级**，避免误判。
- **权限与降级**：若某项检测依赖权限（如读取某路径），在权限不足时应**明确降级**（跳过或标记 WARNING），并打日志说明原因，**避免因权限不足产生大量 WARNING 或误报**。

---

## 三、评分设计与更新

- **分数规则**：`DetectionResult`：`NORMAL`=满分，`WARNING`=一半，`DANGER`=0
- **默认**：`DEFAULT_MAX_SCORE = 10`；核心安全项可提高 maxScore，辅助项可降低
- 新增/修改检测后，须同步调整 maxScore，并确认概览页总分计算正确

---

## 四、误报控制与“不过度、不过时”

**原则**：减少误报，避免极度过时的检测手段，避免因权限不足导致的 WARNING 泛滥。

- **白名单/排除**：特定厂商、机型、模拟器/真机边界的已知正常值，做排除或降级
- **多因子联合**：单指标不足以断定异常时，结合多项证据再下结论（如模拟器 = 硬件属性 + 文件 + 端口等）
- **状态分级**：不确定时用 `STATUS_WARNING` 而非 `STATUS_DANGER`
- **过时手段**：不堆砌已失效或极易误报的检测（如仅靠单一陈旧特征），优先可维护、可解释的逻辑
- **权限不足**：检测前可判断权限/能力，无法安全执行时跳过或降级并打日志，避免无意义的 WARNING

---

## 五、设计思路：考虑全面，不堆“屎山”

- **考虑全面**：设计检测时从「正常设备、定制 ROM、模拟器、真 Root/真 Frida」等多场景考虑，逻辑清晰、可读可维护。
- **复用与结构**：新检测尽量复用现有 `thread_detector`、`port_scanner`、`env_detector` 等模块，避免复制粘贴、重复造轮子。
- **日志清晰**：关键分支、异常路径、降级原因、权限不足等都要**打清楚日志**（便于用 SentryTag 等标签在后台筛选），方便 PM 与开发者排查、改进。

---

## 六、不确定时的行为准则：详细抛出问题 + 第一性原理

**原则**：当**不确定是否应该这样写**、或对实现方式存疑时，**以解决问题为出发点**，把问题**详细、结构化地抛出**，并尽量**从第一性原理出发**说明，便于 PM 判断或协助搜索，避免猜着写、写错再返工。

### 6.1 为什么要“详细抛出问题”

- 模糊的提问只能得到模糊的结论；**问题描述越具体，越容易得到可执行的答案或搜索关键词**。
- 开发/助手应**主动写出**：当前在做什么、卡在哪、已尝试什么、不确定点是什么、需要谁做哪类决策或搜索。

### 6.2 什么叫“从第一性原理出发”

- **第一性原理**：从**事实与约束**出发推理，而不是从“别人都这么写”或“感觉应该这样”。
- 抛出问题时尽量包含：
  - **事实**：当前环境（系统版本、厂商、现象）、代码片段、日志或报错原文。
  - **约束**：项目规范（如必须 Native、必须 syscall）、兼容性要求、权限边界。
  - **目标**：要达成什么结果（例如“在不增加误报的前提下区分某厂商正常行为”）。
  - **不确定点**：具体哪一步、哪一句、哪种写法不确定，以及为什么犹豫（例如“用 A 怕兼容性，用 B 怕被 hook”）。
- 这样 PM 或后续搜索可以基于**事实+约束+目标**做判断，而不是在信息不足下猜。

### 6.3 落地方式（与联系 PM 结合）

- **不确定且需查资料/决策时**：必须联系 PM，并**按 6.2 的结构**把问题写清楚（可贴代码、日志、现象）。
- **明确提出需要搜索的问题**：若需网络搜索，写出你认为应搜索的关键问题或关键词，便于 PM 执行或补充。
- **以解决问题为出发点**：提问的目的是“把这件事做对”，而不是“把代码先写上去”；宁可先停一步、把问题抛清楚，再动笔。

### 6.4 同一问题超过 3 次未解决：换思路或查资料 / 向 PM 提问

- **原则**：若针对**同一问题**已尝试**超过 3 次**仍未解决，应主动**换一种思路**，而不是在同一思路上反复试。
- **可选动作**：
  - **换思路**：重新拆解问题、换实现方式或降级方案（例如先做简化版、或换 Native/Java 分工）。
  - **上网查资料**：按 6.2 整理好事实与目标后，搜索类似实现、官方文档、AOSP/厂商源码等，再决定怎么写。
  - **向 PM 提问**：把已尝试的 3 次（做了什么、结果如何）、当前卡点、以及“是否需要换思路/查哪类资料”一并说明，由 PM 协助决策或提供信息。
- **目的**：避免在同一死胡同里消耗过多时间，通过换思路或外部信息尽快推进问题。

---

## 七、调试与改进：运行日志（SentryTag）

- 开发与排查时，**可向 PM 获取运行日志**（含 **SentryTag** 等标签）。
- PM 可将日志复制给 AI 助手，用于**调试、复现问题、改进检测逻辑与误报**。
- 因此代码中**关键路径须打清日志**，并保持标签/格式便于在 Sentry 或日志系统中筛选。

---

## 八、开发检查清单

新增或修改检测前，确认：

1. [ ] 是否能放在 Native 层？若在 Java 层，是否有充分理由？
2. [ ] 是否使用 syscall / `syscall_utils` 的 `my_*` 而非直接调用 libc？
3. [ ] 是否考虑 Android 10～16 及多厂商兼容性、权限与降级？
4. [ ] 是否设定合理 maxScore，并验证总分计算正确？
5. [ ] 是否评估误报与过时手段，避免权限不足导致的 WARNING 泛滥？
6. [ ] 关键分支与降级是否打了清晰日志（便于 SentryTag 排查）？
7. [ ] 不确定或需查资料时，是否已按「第一性原理」把问题详细抛出（事实、约束、目标、不确定点），并向 PM 提出明确问题或搜索需求？
8. [ ] 若同一问题已尝试超过 3 次未解决，是否已换思路、上网查资料或向 PM 提问，而不是继续在同一方式上重复试？
