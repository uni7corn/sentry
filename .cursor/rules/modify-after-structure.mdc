---
description: 修改代码前先读取项目结构 Skill，重大变更后须更新结构 Skill
alwaysApply: true
---

# 修改前读结构、重大变更后更新结构

## 修改前必做

在修改本项目任何代码或资源之前：

1. **先读取并熟悉项目结构**：打开并阅读 `.cursor/skills/sentry-project-structure/SKILL.md`，了解目录树、关键路径、包名与 Native 库名、扩展时需改动的文件位置。
2. **再动手修改**：在熟悉当前结构的前提下进行编辑、新增或删除，避免改错路径或遗漏配套修改（如 JNI、CMake、strings、Manifest）。

## 重大变更后必做

若本次修改包含以下任一情况，视为**较大变动**，修改完成后必须**更新结构 Skill**（以及必要时项目根目录的 `PROJECT_ARCHITECTURE.md`、`ARCHITECTURE_QUICKREF.md`）：

- 新增或删除模块、包、重要 Java 类或 C++ 检测器
- 目录层级或关键文件路径变更
- 新增/删除检测项、Activity 或入口
- 构建配置变更（如 CMake 源列表、新 ABI、Gradle 模块）

**更新方式**：编辑 `.cursor/skills/sentry-project-structure/SKILL.md`，使其中的「目录树」和「关键路径速查」与当前仓库一致；若架构或扩展流程变化，同步更新 `PROJECT_ARCHITECTURE.md` / `ARCHITECTURE_QUICKREF.md`。

这样可保证后续 Agent 与开发者始终基于最新结构执行维护与更新。
